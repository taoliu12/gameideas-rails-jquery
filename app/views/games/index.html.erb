

<h1>All games</h1>
<p>Order: Oldest to Newest | <%= link_to 'Newest to Oldest', games_newest_to_oldest_path %></p>

<ol>
<% @games.each do |g| %>
    <li> <%= link_to g.title, game_path(g) %> </li>
<% end %>
</ol>

Games you made suggestions to:
<ol>
<% current_user.suggested_games.uniq.each do |g| %>
    <li> <%= link_to g.title, game_path(g) %></li>
<% end %>
</ol>
<h3><%= link_to 'Make a new game', new_game_path %></h3>

Logged in as <%= current_user.username %> | <%= link_to "Logout", logout_path %>
